import { InputProps } from "@/common/types/input";
import { StyledInput } from "@/styles/components/cart";

export default function InputComponent(props: InputProps) {
    return <StyledInput {...props} />;
}
